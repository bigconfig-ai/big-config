review and update all documentation including index.html CLAUDE.md README.md and all the docstring of clojure
make sure thate:
- this syntax is not valida anymore "bb render lock tofu:init tofu:apply -- alpha prod"
- the new syntax is "bb render lock tofu:init tofu:apply -- <raw-command>"
- don't use step.clj. it has been deprecated and replaced by workflow.clj
- the invocation forms are only (wf) and (wf step-fns and opts)
- pluggable/handle-step is [f step step-fns opts]
- push to main because of Trunk-based development
